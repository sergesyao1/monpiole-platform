import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("Property amenities OpenAPI",()=>{it("publishes reusable private and public contracts",()=>{const document=JSON.parse(readFileSync(new URL("../../engineering/contracts/http/openapi.json",import.meta.url),"utf8")) as {paths:Record<string,unknown>;components:{schemas:Record<string,unknown>}};expect(document.paths).toHaveProperty("/v1/amenities");expect(document.paths).toHaveProperty("/v1/properties/{propertyId}/amenities");expect(document.components.schemas).toHaveProperty("AmenityCatalogResponse");expect(JSON.stringify(document.components.schemas.PublicPropertyDetail)).toContain("amenities");});});
