import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("Property viewings OpenAPI",()=>{it("publishes private inquiry-centred viewing contracts without tenant identifiers",()=>{const d=JSON.parse(readFileSync(new URL("../../engineering/contracts/http/openapi.json",import.meta.url),"utf8"))as{paths:Record<string,unknown>;components:{schemas:Record<string,unknown>}};expect(d.paths).toHaveProperty("/v1/properties/{propertyId}/inquiries/{inquiryId}/viewing");expect(d.paths).toHaveProperty("/v1/properties/{propertyId}/viewings/{viewingId}");expect(d.paths).toHaveProperty("/v1/properties/{propertyId}/viewings/{viewingId}/schedule");expect(d.paths).toHaveProperty("/v1/properties/{propertyId}/viewings/{viewingId}/completion");expect(d.paths).toHaveProperty("/v1/properties/{propertyId}/viewings/{viewingId}/cancellation");expect(JSON.stringify(d.components.schemas.ViewingDto)).not.toContain("tenantId")});});
