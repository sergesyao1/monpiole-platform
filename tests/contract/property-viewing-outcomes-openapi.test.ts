import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("Property viewing outcomes OpenAPI",()=>{it("publishes only private tenant-free outcome contracts",()=>{const d=JSON.parse(readFileSync(new URL("../../engineering/contracts/http/openapi.json",import.meta.url),"utf8"))as{paths:Record<string,unknown>;components:{schemas:Record<string,unknown>}};const base="/v1/properties/{propertyId}/viewings/{viewingId}/outcome";expect(d.paths).toHaveProperty(base);expect(d.paths).toHaveProperty(`${base}/proceed`);expect(d.paths).toHaveProperty(`${base}/decline`);expect(JSON.stringify(d.components.schemas.OutcomeDto)).not.toContain("tenantId")});});
