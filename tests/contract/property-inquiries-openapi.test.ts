import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("Property inquiries OpenAPI",()=>{it("separates public submission and private management contracts",()=>{const d=JSON.parse(readFileSync(new URL("../../engineering/contracts/http/openapi.json",import.meta.url),"utf8"))as{paths:Record<string,unknown>;components:{schemas:Record<string,unknown>}};expect(d.paths).toHaveProperty("/v1/public/properties/{publicPropertyId}/inquiries");expect(d.paths).toHaveProperty("/v1/properties/{propertyId}/inquiries");expect(d.paths).toHaveProperty("/v1/properties/{propertyId}/inquiries/{inquiryId}/acknowledgement");expect(JSON.stringify(d.components.schemas.PublicResponseDto)).not.toContain("tenantId");});});
