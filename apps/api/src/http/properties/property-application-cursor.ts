import{Buffer}from"node:buffer";import type{PropertyApplicationCursor}from"@monpiole/property-management";export function encodePropertyApplicationCursor(c:PropertyApplicationCursor){return Buffer.from(JSON.stringify(c)).toString("base64url")}export function decodePropertyApplicationCursor(v:string):PropertyApplicationCursor{try{const x:unknown=JSON.parse(Buffer.from(v,"base64url").toString("utf8"));if(!x||typeof x!=="object"||!("createdAt"in x)||!("applicationId"in x)||typeof x.createdAt!=="string"||typeof x.applicationId!=="string"||!Number.isFinite(Date.parse(x.createdAt)))throw new Error();return{createdAt:x.createdAt,applicationId:x.applicationId}}catch{throw new Error("Invalid Property application cursor")}}
