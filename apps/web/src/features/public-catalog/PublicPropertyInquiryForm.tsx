import{type FormEvent,useRef,useState}from"react";import{ApiProblem}from"../../infrastructure/http/problem-details.js";import{Alert,Button,Field}from"../../ui/index.js";import type{PublicPropertyApi}from"./public-property-api.js";
export function PublicPropertyInquiryForm({propertyId,api}:Readonly<{propertyId:string;api:PublicPropertyApi}>){const[key,setKey]=useState(()=>crypto.randomUUID());const[state,setState]=useState<"idle"|"saving"|"success"|"unavailable"|"error">("idle");const form=useRef<HTMLFormElement>(null);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(state==="saving")return;const data=new FormData(e.currentTarget);if(!data.get("email")&&!data.get("phoneNumber")){setState("error");return;}setState("saving");try{await api.submitInquiry(propertyId,{contactName:String(data.get("contactName")),...optional(data,"email"),...optional(data,"phoneNumber"),...optional(data,"message"),consent:true,consentVersion:"inquiry-v1",idempotencyKey:key});setState("success");form.current?.reset();setKey(crypto.randomUUID());}catch(error){setState(error instanceof ApiProblem&&error.problem.status===404?"unavailable":"error");}}return <section className="public-inquiry"><h2>Ce bien vous intéresse ?</h2><p>Envoyez votre demande au gestionnaire du bien.</p>{state==="success"&&<Alert tone="success" title="Demande envoyée"><p>Le gestionnaire pourra vous recontacter.</p></Alert>}{state==="unavailable"&&<Alert tone="warning" title="Bien indisponible"><p>Ce bien n’est plus ouvert aux demandes.</p></Alert>}{state==="error"&&<Alert tone="danger" title="Demande non envoyée"><p>Vérifiez vos coordonnées puis réessayez.</p></Alert>}<form ref={form} className="form-stack" onSubmit={(e)=>void submit(e)}><Field label="Nom"><input name="contactName" required maxLength={200}/></Field><div className="form-grid"><Field label="Adresse e-mail" optional><input name="email" type="email" maxLength={320}/></Field><Field label="Téléphone" optional><input name="phoneNumber" type="tel" maxLength={100}/></Field></div><Field label="Message" optional><textarea name="message" maxLength={2000}/></Field><label className="checkbox-field"><input name="consent" type="checkbox" required/>J’accepte que mes coordonnées soient utilisées pour répondre à cette demande.</label><Button type="submit" loading={state==="saving"} loadingLabel="Envoi…">Envoyer ma demande</Button></form></section>}
function optional(data:FormData,key:string):Record<string,string>{const value=String(data.get(key)??"").trim();return value?{[key]:value}:{};}
