import{render,screen}from"@testing-library/react";import{describe,expect,it,vi}from"vitest";import{PropertyInquiriesSection}from"./PropertyInquiriesSection.js";describe("Property inquiries management",()=>{it("shows the empty state",async()=>{const api={listPropertyInquiries:vi.fn(async()=>({items:[],pageInfo:{hasNextPage:false,nextCursor:null}})),acknowledgePropertyInquiry:vi.fn(),closePropertyInquiry:vi.fn(),retrieveInquiryViewing:vi.fn(),schedulePropertyViewing:vi.fn(),reschedulePropertyViewing:vi.fn(),completePropertyViewing:vi.fn(),cancelPropertyViewing:vi.fn(),retrievePropertyViewingOutcome:vi.fn(),createPropertyViewingOutcome:vi.fn(),proceedPropertyViewingOutcome:vi.fn(),declinePropertyViewingOutcome:vi.fn()};render(<PropertyInquiriesSection propertyId="property" api={api}/>);expect(await screen.findByText("Aucune demande")).toBeVisible();});});
